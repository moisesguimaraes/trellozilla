# The order of packages is significant, because pip processes them
# in the order of appearance. Changing the order has an impact on
# the overall integration process, which may cause wedges later.

pbr!=2.1.0,>=2.0.0 # Apache-2.0
oslo.config>=6.11.0 # Apache-2.0
python-bugzilla>=2.2.0 # Apache-2.0
py-trello>=0.15.0 # BSD
